import 'dotenv/config.js'; import { forgotPassword } from './controllers/authController.js'; async function run() { const req = { body: { email: 'godswillnwafor@mtu.edu.ng' }, headers: {}, ip: '127.0.0.1' }; const res = { status: (s) => ({ json: (j) => console.log('STATUS JSON:', s, j) }), json: (j) => console.log('JSON:', j) }; try { await forgotPassword(req, res); } catch (e) { console.error('BIG CATCH:', e); } } run();
